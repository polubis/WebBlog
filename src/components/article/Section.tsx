import styled from "styled-components"

import { XL, M } from "../../ui"

export default styled.section`
  margin-bottom: 48px;

  ${XL} {
    margin-bottom: 32px;
  }

  ${M} {
    margin-bottom: 12px;
  }
`
