import React from "react"
import Layout from "../../containers/Layout"
import { useLayoutProvider } from "../../providers/LayoutProvider"

const MentoringView = () => {
  const layout = useLayoutProvider()

  return <Layout>sema</Layout>
}

export { MentoringView }
